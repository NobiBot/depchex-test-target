[project]
name = "depchex-test-target"
version = "0.1.0"
description = "Test target for DepChex dependency confusion scanner"
requires-python = ">=3.9"
dependencies = [
    "alembic==1.12.1",
    "click==8.1.7",
    "gunicorn==21.2.0",
    "celery==5.3.4",
    "redis==5.0.1",
    "acme-internal-auth",
    "myco-secret-sauce",
]
